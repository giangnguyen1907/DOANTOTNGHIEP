# You can find more information about this file on the symfony website: dung cho thiet lap man hinh
# http://www.symfony-project.org/reference/1_4/en/08-Security
default:
  is_secure:   true # Yeu cau toan bo ung dung can phai dang nhap

all:
  is_secure: true

# quyen cua action
# Hien thi danh sach
index:
  is_secure: true
  credentials: [[PS_STUDENT_ATTENDANCE_SHOW,PS_STUDENT_ATTENDANCE_FILTER_SCHOOL,PS_STUDENT_ATTENDANCE_EDIT,PS_STUDENT_ATTENDANCE_ADD,PS_STUDENT_ATTENDANCE_DELETE]]   

new:
  is_secure: true
  credentials: [PS_STUDENT_ATTENDANCE_ADD]

statistic:
  is_secure: true
  credentials: [PS_STUDENT_ATTENDANCE_STATISTIC]

create:
  is_secure: true
  credentials: [PS_STUDENT_ATTENDANCE_ADD]

edit:
  is_secure: true
  credentials: [PS_STUDENT_ATTENDANCE_EDIT]

update:
  is_secure: true
  credentials: [PS_STUDENT_ATTENDANCE_EDIT]

delete:
  is_secure: true
  credentials: [PS_STUDENT_ATTENDANCE_DELETE]